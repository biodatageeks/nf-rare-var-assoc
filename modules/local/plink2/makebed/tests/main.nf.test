// TODO nf-core: Once you have added the required tests, please run the following command to build this file:
// nf-core modules test plink2
nextflow_process {

    name "Test Process PLINK2_MAKEBED"
    script "../main.nf"
    process "PLINK2_MAKEBED"

    tag "modules"
    tag "modules_"
    tag "plink2_MAKEBED"

    // TODO nf-core: Change the test name preferably indicating the test-data and file-format used
    test("small vcf - bed,bim,fam") {

        // TODO nf-core: If you are created a test for a chained module
        // (the module requires running more than one process to generate the required output)
        // add the 'setup' method here.
        // You can find more information about how to use a 'setup' method in the docs (https://nf-co.re/docs/contributing/modules#steps-for-creating-nf-test-for-chained-modules).

        when {
            process {
                """
                input[0] = [
                    [ id:'test', single_end:false ], // meta map
                    file(params.modules_testdata_base_path + 'genomics/homo_sapiens/illumina/plink/test.rnaseq.bed', checkIfExists: true),
                    file(params.modules_testdata_base_path + 'genomics/homo_sapiens/illumina/plink/test.rnaseq.bim', checkIfExists: true),
                    file(params.modules_testdata_base_path + 'genomics/homo_sapiens/illumina/plink/test.rnaseq.fam', checkIfExists: true),
                    [],
                    []
                ]
                input[1] = 'filter'
                input[2] = '--hwe 1e-13 0.001'
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                // { assert snapshot(process.out).match() }
                //TODO nf-core: Add all required assertions to verify the test output.
                // See https://nf-co.re/docs/contributing/tutorials/nf-test_assertions for more information and examples.
            )
        }

    }


    /*
    test("split-par - minimal_with_X vcf") {

        when {
            process {
                """
                input[0] = [
                    [ id:'test', single_end:false ], // meta map
                    [],  // bed
                    [],  // bim
                    [],  // fam
                    file('${projectDir}/assets/small_data/minimal_with_X.vcf', checkIfExists: true),
                    []   // frq
                ]
                input[1] = 'split_par'
                input[2] = '--double-id --vcf-half-call missing --split-par b38'
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                // { assert snapshot(process.out).match() }
                //TODO nf-core: Add all required assertions to verify the test output.
                // See https://nf-co.re/docs/contributing/tutorials/nf-test_assertions for more information and examples.
            )
        }

    }

    test("impute-sex - minimal_with_X vcf") {

        setup {
            //run("PLINK19_MAKEBED") {
            //    script "../../../plink19/makebed/main.nf"
            //    process {
            //        """
            //        input[0] = [
            //            [ id:'test', single_end:false ], // meta map
            //            file('${projectDir}/assets/small_data/minimal_with_X.vcf', checkIfExists: true)
            //        ]
            //        """
            //    }
            //}
            run("BCFTOOLS_VCF2FRQ") {
                script "../../../bcftools/vcf2frq/main.nf"
                process {
                    """
                    input[0] = [
                        [ id:'test', single_end:false ], // meta map
                        file('${projectDir}/assets/small_data/minimal_with_X.vcf', checkIfExists: true),
                        []
                    ]
                    """
                }
            }
        }

        when {
            // PLINK19_MAKEBED.out.out_bed_bim_fam[0],
            // PLINK19_MAKEBED.out.out_bed_bim_fam[1],
            // PLINK19_MAKEBED.out.out_bed_bim_fam[2],
            // PLINK19_MAKEBED.out.out_bed_bim_fam[3],
            process {
                """
                input[0] = [
                    [ id:'test', single_end:false ],
                    file('${projectDir}/modules/local/plink2/makebed/tests/assets/test.bed', checkIfExists: true),
                    file('${projectDir}/modules/local/plink2/makebed/tests/assets/test.bim', checkIfExists: true),
                    file('${projectDir}/modules/local/plink2/makebed/tests/assets/test.fam', checkIfExists: true),
                    [],
                    BCFTOOLS_VCF2FRQ.out.frq[1]   // frq
                ]
                input[1] = 'impute_sex'
                input[2] = '--impute-sex'
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                // { assert snapshot(process.out).match() }
                //TODO nf-core: Add all required assertions to verify the test output.
                // See https://nf-co.re/docs/contributing/tutorials/nf-test_assertions for more information and examples.
            )
        }

    }

    test("impute-sex - minimal_with_X vcf") {

        setup {
            run("BCFTOOLS_VCF2FRQ") {
                script "../../../bcftools/vcf2frq/main.nf"
                process {
                    """
                    input[0] = [
                        [ id:'test', single_end:false ], // meta map
                        file('${projectDir}/assets/small_data/minimal_with_X.vcf', checkIfExists: true),
                        []
                    ]
                    """
                }
            }
        }

        when {
            process {
                """
                input[0] = [
                    [ id:'test', single_end:false ], // meta map
                    [],  // bed
                    [],  // bim
                    [],  // fam
                    file('${projectDir}/assets/small_data/minimal_with_X.vcf', checkIfExists: true),
                    BCFTOOLS_VCF2FRQ.out.frq[1]   // frq
                ]
                input[1] = 'impute_sex'
                input[2] = '--double-id --vcf-half-call missing --split-par b38 --impute-sex'
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                // { assert snapshot(process.out).match() }
                //TODO nf-core: Add all required assertions to verify the test output.
                // See https://nf-co.re/docs/contributing/tutorials/nf-test_assertions for more information and examples.
            )
        }

    }
    */
}
