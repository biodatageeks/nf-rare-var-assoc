process {
    withName: 'BCFTOOLS_ANNOTATE' {
        // output-type v (plain VCF) so test assertions can read the header as text
        ext.args   = "-x ID,INFO/DP,FORMAT/DP --output-type v"
        ext.prefix = { "${meta.id}_ann" }
    }
}
