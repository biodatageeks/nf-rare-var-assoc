nextflow_workflow {

    name "RARE_VAR_ASSOC semantic injected signal"
    script "main.nf"
    workflow "PSUSZYNS_RARE_VAR_ASSOC"

    tag "workflows"
    tag "semantic"
    tag "signal"

    // NOTE:
    // This is a template for Phase 2 (scenario S10).

    test("S10 injected signal detectability") {

        // TODO:
        // Assert expected injected signal rank/hit from fixture contract.

        when {
            params {
                // TODO replace with synthetic fixture paths
                input_vcf = "${projectDir}/assets/medium_data/prepared_chr_12_22_X_csq_filtered_2k_rand_3k.vcf.gz"
                input_cases = "${projectDir}/assets/medium_data/1kGP_cases_200.txt"
                input_controls = "${projectDir}/assets/medium_data/1kGP_controls_400.txt"
                skip_preparation = true
                skip_reporting = true
                use_dosage = true
                outdir = "${projectDir}/tests/workflows/expected/s10_signal"
                project_name = "semantic_s10_signal"
            }
        }

        then {
            assertAll(
                { assert true }
            )
        }
    }
}
