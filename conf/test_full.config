/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running full-size tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a full size pipeline test.

    Use as follows:
        nextflow run psuszyns/rare-var-assoc-nf -profile test_full,<docker/singularity> --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

params {
    config_profile_name        = 'Full test profile'
    config_profile_description = 'Full test dataset to check pipeline function'

    plink2_write_snplist_options    = '--mac 100'
    plink2_write_snplist_qc_options = '--maf 0.01 --mac 100 --geno 0.1 --hwe 1e-15 --mind 0.1 --write-samples'

    rscript_vcf2aaf_options         = 'AF_EUR AF'

    // Input data for full size test
    input_vcf  = "${projectDir}/assets/medium_data/1kGP.chr22.panel_head100k_exome.vcf"
    input_controls = "${projectDir}/assets/medium_data/1kGP_controls.txt"
    input_cases = "${projectDir}/assets/medium_data/1kGP_cases.txt"
    project_name = "test"

}
